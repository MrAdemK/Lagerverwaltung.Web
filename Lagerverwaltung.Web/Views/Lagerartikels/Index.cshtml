@model IEnumerable<Lagerverwaltung.Model.DatabaseModels.Lagerartikel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.L_Bezeichnung)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.L_Preis)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lagerstand)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.L_MengenEinheit)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.L_Bezeichnung)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.L_Preis)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lagerstand)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.L_MengenEinheit)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LagerartikelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LagerartikelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LagerartikelId })
        </td>
    </tr>
}

</table>
